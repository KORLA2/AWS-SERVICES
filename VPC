
VPC (Virtual Private Cloud) is isolated network in aws where we can deploy our aws resources .

## NACL

Network Access Control List is like fire wall at the subnet level.

NACLs are stateless which means there are both inbound, out bound rules defined.

NACL rules are evaluated from lowest number (More priority) to highest number.

NACL rules are applied to all the instances with in subnet.

